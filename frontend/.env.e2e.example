# Copy to .env.e2e for manual `npm run dev -- --mode e2e` sessions.
# Playwright E2E tests do not need this file — vars are set in playwright.config.ts.

VITE_E2E_BYPASS_AUTH=true
VITE_SUPABASE_URL=https://example.invalid
VITE_SUPABASE_ANON_KEY=fake-supabase-anon-key-not-a-real-jwt
